<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'watch to code', root_path, class: 'navbar-brand logo' %>
      </div>
      <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Popular", videos_path %>
        <% Category.all.order(:name).each do |category| %>
          <li><%= link_to category.name.to_s.titleize, video_category_path(category) %></li>
        <% end %>
        <% if user_signed_in? and current_user.role == "admin" %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "New Video", new_video_path %></li>            
            <li><%= link_to "Categories", categories_path %></li>
            <li><%= link_to "Levels", levels_path %></li>
          </ul>
        </li>
        <% end %>      
      </ul>
    </div>
  </div>
</div>